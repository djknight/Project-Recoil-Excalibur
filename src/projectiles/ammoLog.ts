//Ammo-Weapons Log
const Shell = {
  //shell
  ShellAp: 10,
  ShellDmg: 10,
  shellSpeed: +1000,
  setTimeout: 1000,
};
const rocket = {
  //rocket
  rocketAoe: 20,
  rocketDmg: 10,
  rocketSpeed: +800,
  // rocket_setTimeout: 1500,
  setTimeout: 1500,
};

const missile = {
  //missile
  missileSpeed: +1200,
  missileDmg: 30,
  setTimeout: 10000,
};

/// since const use normal Time out and Dmg?
//is speed also the same?/
//is setTimeout?
